<?php
require("PHPMailer_5.2.0/class.phpmailer.php");

$mail = new PHPMailer();

$mail->IsSMTP();                                      // set mailer to use SMTP
$mail->Host = "webclinicmailer.co.uk";  // specify main and backup server
$mail->SMTPAuth = true;     // turn on SMTP authentication
$mail->Username = "newsletter@bshop.webclinicmailer.co.uk";  // SMTP username
$mail->Password = "YuxZQA@"; // SMTP password

$mail->From = "latheesh Sender <latheesh.george@thewebclinic.co.uk>";
$mail->FromName = "Mailer";
$mail->AddAddress("latheeshgeorge@gmail.com", "latheeshg");
$mail->AddAddress("latheesh.george@thewebclinic.co.uk"); 
$mail->AddBCC("raghunath686@gmail.com","Raghu");
                 // name is optional
$mail->AddReplyTo("latheesh.george@thewebclinic.co.uk", "Reply");

$mail->Subject = "Here is the subject";
$mail->Body    = "This is the HTML message body <b>in bold!</b>";
$mail->AltBody = "This is the body in plain text for non-HTML mail clients";

if(!$mail->Send())
{
   echo "Message could not be sent. <p>";
   echo "Mailer Error: " . $mail->ErrorInfo;
   exit;
}

echo "Message has been sent";
?>
